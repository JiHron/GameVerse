"""backend package marker

This file makes the `backend` folder a Python package so imports like
`import backend` and `backend.main` work reliably when uvicorn imports
the application module.
"""

# (intentionally empty — package marker)
